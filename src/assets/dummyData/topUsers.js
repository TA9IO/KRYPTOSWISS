import {
  user0,
  user1,
  user2,
  user3,
  user4,
  user5,
  user6,
  user7,
  user8,
  user9,
} from "../images/userPP";
import eth from '../images/eth.webp';

export const topUsers = [
  {
    img: user0,
    name: "takiDDIne B-)",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user1,
    name: "Alberto Suit",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user2,
    name: "Steven Oi",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user3,
    name: "Funny Clown",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user4,
    name: "Wolfy Stei",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user5,
    name: "Sarah Power",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user6,
    name: "Recycling Team",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user7,
    name: "Rosyana",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user8,
    name: "Queensa",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },
  {
    img: user9,
    name: "Angelica",
    eth: eth,
    sells: (Math.random() * 100 + 90).toFixed(2),
    currency: 'ETh',
    //dont use this method for to generate keys i use just to remove the err since this is dummy data
    id: Math.random().toString(32).slice(2, 20),
  },

];
